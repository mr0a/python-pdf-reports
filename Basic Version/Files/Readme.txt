To run the exe file, just press enter to load with the already given dummy data.

To check with different data,
1) Specify the location of New data (ex: ./New Data.xlsx if file is in same directory)
2) Specify the folder of the images (ex: ./images if the images are in images directory)

Note: Do not delete required files